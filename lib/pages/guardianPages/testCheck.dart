// testCheck.dart : 인지 검사 결과 확인

import 'package:flutter/material.dart';

class TestCheckPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('인지 검사 확인'),
      ),
      body: Center(
        child: Text('인지 검사 결과 확인 페이지'),
      ),
    );
  }
}