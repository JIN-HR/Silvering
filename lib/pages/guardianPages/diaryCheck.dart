// diaryCheck.dart : 일기 확인

import 'package:flutter/material.dart';

class DiaryCheckPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('일기 확인'),
      ),
      body: Center(
        child: Text('일기 확인 페이지'),
      ),
    );
  }
}