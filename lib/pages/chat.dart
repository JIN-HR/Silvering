// chat.dart : 게시판
import 'package:flutter/material.dart';

class TaxiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('전문가 상담'),
      ),
      body: Center(
        child: Text('전문가 상담 페이지'),
      ),
    );
  }
}
